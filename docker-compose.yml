version: "3"
services:
    api:
        build: .
        ports:
            - 8000:8000
        environment:
            - DATABASE_HOSTNAME=localhost
            - DATABASE_PORT=5432
            - DATABASE_PASSWORD=root
            - DATABASE_NAME=NutriAPI
            - DATABASE_USERNAME=postgres